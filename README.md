# customizable-presentation# Slide Generator API

## Overview

Generate customizable `.pptx` presentations using FastAPI and python-pptx.

## Setup

```bash
pip install -r requirements.txt
uvicorn app.main:app --reload
